spring:
  application:
    name: contratos
  datasource:
    url: jdbc:postgresql://localhost:5433/correspondentedb
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect